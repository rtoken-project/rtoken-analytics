{
  "name": "rdai-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ send-tribute/rdai-graph",
    "create-local": "graph create --node http://localhost:8020/ send-tribute/rdai-graph",
    "remove-local": "graph remove --node http://localhost:8020/ send-tribute/rdai-graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 send-tribute/rdai-graph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.16.1",
    "@graphprotocol/graph-ts": "0.16.0"
  }
}
